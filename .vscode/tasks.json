{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "go build ./cmd/go-gatest (-Og -g)",
			"type": "shell",
			"command": "make build",
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": "$go",
			"group": "build",
			"presentation": {
				"reveal": "silent",
				"panel": "shared"
			}
		}
	]
}
